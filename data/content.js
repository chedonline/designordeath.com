/* DESIGN OR DEATH — content pools.
   Loaded as a plain script (not fetched JSON) so index.html works from
   file:// with no server — same convention as rapscore/citydrawer. */
const DOD_CONTENT = {
  // KERN! — short, legible words with interior letters worth spacing.
  kern: {
    words: [
      "TYPE", "DESIGN", "KERNING", "LAYOUT", "SPACING", "GRID", "SERIF",
      "PIXEL", "MARGIN", "BASELINE", "WEIGHT", "LEADING", "VECTOR",
      "CANVAS", "FORMAT", "RHYTHM", "STROKE", "GLYPH", "MOTION", "CONTRAST"
    ]
  }
};
